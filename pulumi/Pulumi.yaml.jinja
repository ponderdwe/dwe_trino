name: {{ project_name }}
description: DWE Trino Query Engine for {{ project_name }}
runtime:
  name: python
  options:
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: dwe-trino
